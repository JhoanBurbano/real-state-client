import { LoginForm } from '@/components/auth/LoginFormNew'

export default function LoginPage() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-surface to-surface-elev flex items-center justify-center p-4">
      <LoginForm />
    </div>
  )
}
